# Финальная настройка проекта

## ✅ Что уже сделано

1. ✅ Исправлен `package.json` - удален несуществующий пакет `@radix-ui/react-table`
2. ✅ Зависимости установлены (`npm install` выполнен успешно)
3. ✅ Все скрипты для БД готовы

## ⚠️ Что нужно сделать вручную

### Шаг 1: Настройте DATABASE_URL в .env.local

Откройте файл `.env.local` и добавьте/проверьте строку:

```env
DATABASE_URL=postgresql://user:password@host:port/database
NEXT_PUBLIC_SITE_URL=http://localhost:3000
```

**Примеры DATABASE_URL:**

1. **Локальная PostgreSQL:**
   ```env
   DATABASE_URL=postgresql://postgres:password@localhost:5432/goodsindexuz
   ```

2. **Neon (Serverless):**
   ```env
   DATABASE_URL=postgresql://user:pass@ep-xxx.us-east-2.aws.neon.tech/dbname?sslmode=require
   ```

3. **Railway:**
   ```env
   DATABASE_URL=postgresql://postgres:pass@containers-us-west-xxx.railway.app:5432/railway
   ```

4. **Supabase (как обычный PostgreSQL):**
   ```env
   DATABASE_URL=postgresql://postgres.xxx:password@aws-0-us-east-1.pooler.supabase.com:6543/postgres
   ```

### Шаг 2: Выполните настройку БД

После настройки `DATABASE_URL` выполните:

```bash
npm run db:setup
```

Этот скрипт автоматически:
- ✅ Проверит подключение к БД
- ✅ Выполнит миграции (если таблиц нет)
- ✅ Создаст админ-пользователя (admin/admin)
- ✅ Проверит категории
- ✅ Покажет статистику

### Шаг 3: Запустите проект

```bash
npm run dev
```

Откройте:
- Публичный сайт: http://localhost:3000
- Админ-панель: http://localhost:3000/admin/login
  - Email: `admin`
  - Password: `admin`

## 📋 Альтернативные команды

Если `npm run db:setup` не работает:

```bash
# Установите tsx глобально
npm install -g tsx

# Затем запускайте напрямую
tsx scripts/setup-database.ts
```

Или пошагово:

```bash
# 1. Миграции
tsx scripts/run-migrations.ts

# 2. Админ-пользователь
tsx scripts/create-admin-user.ts

# 3. Менеджер (опционально)
tsx scripts/create-default-manager.ts
```

## ✅ Статус проекта

- ✅ Проект инициализирован
- ✅ Зависимости установлены
- ✅ Миграция на PostgreSQL завершена
- ✅ Все скрипты готовы
- ⏳ Осталось: настроить DATABASE_URL и выполнить `npm run db:setup`

## 🎯 После настройки БД

1. Войдите в админ-панель
2. Создайте менеджера по умолчанию (если не создали через скрипт)
3. Добавьте продукты через админ-панель
4. Проект готов к использованию!

