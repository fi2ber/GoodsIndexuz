# Пошаговая инструкция по установке

## ⚠️ Важно: Проблема с npm

Если `npm install` выдает ошибку с логами, это системная проблема с правами доступа. Решения:

### Решение 1: Использовать yarn
```bash
yarn install
```

### Решение 2: Исправить права npm
```bash
sudo chown -R $(whoami) ~/.npm
npm install
```

### Решение 3: Установить с флагами
```bash
npm install --no-audit --no-fund --legacy-peer-deps
```

---

## ✅ Шаг 1: Установка зависимостей

```bash
npm install
```

Или используйте yarn:
```bash
yarn install
```

**Проверка**: Должна появиться папка `node_modules`

---

## ✅ Шаг 2: Настройка Supabase

### 2.1 Создайте проект
1. Откройте https://supabase.com
2. Войдите или зарегистрируйтесь
3. Нажмите "New Project"
4. Заполните данные проекта
5. Дождитесь завершения (1-2 минуты)

### 2.2 Выполните миграции

В Supabase Dashboard:

1. Откройте **SQL Editor** (левый сайдбар)
2. Нажмите **New query**
3. Выполните миграции по порядку:

**Миграция 1: Схема БД**
- Откройте файл: `supabase/migrations/001_initial_schema.sql`
- Скопируйте весь SQL код
- Вставьте в SQL Editor
- Нажмите **Run** (или Ctrl+Enter)

**Миграция 2: Категории**
- Откройте: `supabase/migrations/003_seed_categories.sql`
- Скопируйте и выполните

**Проверка**: В Table Editor должны появиться таблицы `categories`, `products`, `managers`, `inquiries`, `users`

---

## ✅ Шаг 3: Получение ключей API

1. В Supabase Dashboard → **Settings** → **API**
2. Скопируйте значения:

```
Project URL → NEXT_PUBLIC_SUPABASE_URL
anon public → NEXT_PUBLIC_SUPABASE_ANON_KEY  
service_role → SUPABASE_SERVICE_ROLE_KEY (секретный!)
```

---

## ✅ Шаг 4: Создание .env.local

Создайте файл `.env.local` в корне проекта:

```bash
touch .env.local
```

Откройте и вставьте:

```env
NEXT_PUBLIC_SUPABASE_URL=https://ваш-проект-id.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=ваш_anon_ключ
SUPABASE_SERVICE_ROLE_KEY=ваш_service_role_ключ
NEXT_PUBLIC_SITE_URL=http://localhost:3000
```

**Замените** значения на реальные из Supabase!

---

## ✅ Шаг 5: Создание админ-пользователя

### Вариант A: Через скрипт (после настройки .env.local)

```bash
npx tsx scripts/create-admin-user.ts
```

### Вариант B: Через SQL (рекомендуется)

1. **Сгенерируйте хеш пароля:**

```bash
node scripts/generate-password-hash.js admin
```

Или используйте онлайн: https://bcrypt-generator.com/
- Password: `admin`
- Rounds: `10`

2. **В Supabase SQL Editor выполните:**

```sql
INSERT INTO users (email, password_hash, role)
VALUES (
  'admin',
  'ВАШ_СГЕНЕРИРОВАННЫЙ_ХЕШ',
  'admin'
);
```

**Проверка**: В Table Editor → `users` должен появиться пользователь с email `admin`

---

## ✅ Шаг 6: Создание менеджера по умолчанию

В Supabase SQL Editor:

```sql
INSERT INTO managers (name, telegram_username, telegram_link, is_active, is_default)
VALUES (
  'Default Manager',
  'ваш_telegram_username',
  'https://t.me/ваш_telegram_username',
  true,
  true
);
```

**Замените** `ваш_telegram_username` на реальный username (без @)

---

## ✅ Шаг 7: Запуск проекта

```bash
npm run dev
```

Откройте браузер:
- **Публичный сайт**: http://localhost:3000
- **Админ-панель**: http://localhost:3000/admin/login
  - Email: `admin`
  - Password: `admin`

---

## ✅ Шаг 8: Создание продуктов

После входа в админ-панель:

1. Перейдите в **Products** → **New Product**
2. Создайте продукты из вашего списка (18 продуктов)
3. Заполните все поля:
   - Name (RU/EN) - обязательно
   - Category - обязательно
   - Slug - обязательно
   - Остальные поля опциональны

---

## 🔍 Проверка установки

Убедитесь что:

- [ ] `node_modules` существует
- [ ] `.env.local` создан и заполнен
- [ ] Таблицы созданы в Supabase
- [ ] Админ-пользователь создан
- [ ] Менеджер по умолчанию создан
- [ ] Проект запускается: `npm run dev`
- [ ] Можно войти в админ-панель

---

## ❓ Решение проблем

### Ошибка подключения к Supabase
- Проверьте правильность ключей в `.env.local`
- Убедитесь что проект Supabase активен
- Проверьте что миграции выполнены

### Ошибка аутентификации
- Проверьте что пользователь создан: `SELECT * FROM users;`
- Убедитесь что пароль правильно захеширован
- Попробуйте создать пользователя заново

### Ошибки TypeScript/сборки
```bash
rm -rf node_modules .next
npm install
npm run dev
```

### Проблемы с npm
Используйте yarn:
```bash
yarn install
yarn dev
```

