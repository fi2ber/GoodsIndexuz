# Выполнение миграций и создание админ-пользователя

## ⚠️ Важно: Сначала установите зависимости!

```bash
npm install
```

Или если npm не работает:
```bash
yarn install
```

## 🚀 После установки зависимостей

### Вариант 1: Автоматическая настройка (рекомендуется)

Выполните все одним скриптом:

```bash
npm run db:setup
```

Это выполнит:
- ✅ Проверку подключения к БД
- ✅ Выполнение миграций
- ✅ Создание админ-пользователя (admin/admin)
- ✅ Проверку категорий
- ✅ Статистику БД

### Вариант 2: Пошагово

1. **Выполнить миграции:**
   ```bash
   npm run db:migrate
   ```

2. **Создать админ-пользователя:**
   ```bash
   npm run db:admin
   ```

3. **Создать менеджера (опционально):**
   ```bash
   npm run db:manager
   ```

## 🔧 Если npm скрипты не работают

### Используйте tsx напрямую:

```bash
# Установите tsx глобально
npm install -g tsx

# Затем запускайте:
tsx scripts/setup-database.ts
tsx scripts/run-migrations.ts
tsx scripts/create-admin-user.ts
```

### Или используйте yarn:

```bash
yarn install
yarn db:setup
```

## ✅ Проверка результата

После выполнения скриптов:

1. **Проверьте таблицы** (через psql или ваш SQL клиент):
   ```sql
   SELECT table_name FROM information_schema.tables 
   WHERE table_schema = 'public';
   ```
   Должны быть: categories, products, managers, inquiries, users

2. **Проверьте админа:**
   ```sql
   SELECT * FROM users WHERE email = 'admin';
   ```

3. **Проверьте категории:**
   ```sql
   SELECT * FROM categories;
   ```
   Должны быть 3 категории

## 📋 Требования

Перед запуском убедитесь что:

- ✅ Зависимости установлены: `npm install`
- ✅ `.env.local` создан с `DATABASE_URL`
- ✅ PostgreSQL БД доступна и запущена

## 🎯 После настройки

1. Запустите проект:
   ```bash
   npm run dev
   ```

2. Войдите в админ-панель:
   - URL: http://localhost:3000/admin/login
   - Email: `admin`
   - Password: `admin`

3. Создайте менеджера через админ-панель (если не создали через скрипт)

4. Добавьте продукты через админ-панель


