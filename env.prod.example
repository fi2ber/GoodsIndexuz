# Database password (use a strong one!)
DB_PASSWORD=your_strong_password_here

# Your site URL (e.g., https://goodsindex.uz)
SITE_URL=https://your-domain.com

# Database URL for the app (inside docker network)
# Note: In docker-compose.prod.yml we use the variables above,
# but if you need to connect from outside:
# DATABASE_URL=postgresql://postgres:your_strong_password_here@localhost:5432/goodsindexuz
