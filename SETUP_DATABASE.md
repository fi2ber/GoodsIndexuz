# Настройка базы данных - Инструкция

## ⚠️ Важно: Перед запуском скриптов

Убедитесь, что в `.env.local` настроен `DATABASE_URL`:

```env
DATABASE_URL=postgresql://user:password@host:port/database
NEXT_PUBLIC_SITE_URL=http://localhost:3000
```

## 🚀 Быстрый способ (рекомендуется)

Выполните все одним скриптом:

```bash
npm run db:setup
```

Это выполнит:
- ✅ Проверку подключения к БД
- ✅ Выполнение миграций (если еще не выполнены)
- ✅ Создание/обновление админ-пользователя (admin/admin)
- ✅ Проверку категорий
- ✅ Статистику БД

## 📋 Пошаговый способ

### Шаг 1: Выполнить миграции

```bash
npm run db:migrate
```

Или:
```bash
npx tsx scripts/run-migrations.ts
```

Это создаст:
- Все таблицы (categories, products, managers, inquiries, users)
- Индексы
- Триггеры
- RLS политики
- Категории (Орехи, Бобовые, Сухофрукты)

### Шаг 2: Создать админ-пользователя

```bash
npm run db:admin
```

Или:
```bash
npx tsx scripts/create-admin-user.ts
```

Создаст пользователя:
- **Email**: `admin`
- **Password**: `admin`

### Шаг 3: Создать менеджера по умолчанию (опционально)

```bash
npm run db:manager
```

Или с параметрами:
```bash
MANAGER_NAME="Иван Иванов" \
MANAGER_TELEGRAM_USERNAME="ivan_manager" \
MANAGER_TELEGRAM_LINK="https://t.me/ivan_manager" \
npx tsx scripts/create-default-manager.ts
```

## 🔧 Альтернатива: Если npm скрипты не работают

### Используйте node напрямую:

```bash
# Установите tsx глобально (если еще не установлен)
npm install -g tsx

# Затем запускайте скрипты:
tsx scripts/run-migrations.ts
tsx scripts/create-admin-user.ts
tsx scripts/setup-database.ts
```

### Или используйте yarn:

```bash
yarn db:setup
yarn db:migrate
yarn db:admin
```

## ✅ Проверка результата

После выполнения скриптов проверьте:

1. **Таблицы созданы:**
   ```sql
   SELECT table_name FROM information_schema.tables 
   WHERE table_schema = 'public';
   ```
   Должны быть: categories, products, managers, inquiries, users

2. **Админ создан:**
   ```sql
   SELECT * FROM users WHERE email = 'admin';
   ```

3. **Категории созданы:**
   ```sql
   SELECT * FROM categories;
   ```
   Должны быть 3 категории: Орехи, Бобовые, Сухофрукты

## 🎯 После настройки

1. Запустите проект:
   ```bash
   npm run dev
   ```

2. Откройте админ-панель:
   http://localhost:3000/admin/login
   - Email: `admin`
   - Password: `admin`

3. Создайте менеджера через админ-панель (если не создали через скрипт)

4. Добавьте продукты через админ-панель

## ❓ Решение проблем

### Ошибка "DATABASE_URL is not set"
- Проверьте что `.env.local` существует
- Убедитесь что `DATABASE_URL` указан в файле

### Ошибка подключения к БД
- Проверьте правильность `DATABASE_URL`
- Убедитесь что PostgreSQL запущен и доступен
- Проверьте права доступа пользователя

### Ошибка "relation does not exist"
- Выполните миграции: `npm run db:migrate`

### Ошибка с npm/npx
- Используйте `yarn` вместо `npm`
- Или установите `tsx` глобально: `npm install -g tsx`


