# 🚀 Быстрый старт проекта

## Шаг 1: Запустите Docker Desktop

Убедитесь, что **Docker Desktop** запущен на вашем Mac.

Если не установлен:
```bash
brew install --cask docker
```

## Шаг 2: Запустите PostgreSQL в Docker

```bash
npm run docker:setup
```

Эта команда:
- ✅ Запустит PostgreSQL в Docker
- ✅ Создаст базу данных `goodsindexuz`
- ✅ Покажет DATABASE_URL для `.env.local`

## Шаг 3: Обновите .env.local (автоматически)

```bash
npm run env:update
```

Или вручную откройте `.env.local` и убедитесь, что есть:

```env
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/goodsindexuz
NEXT_PUBLIC_SITE_URL=http://localhost:3000
```

## Шаг 4: Настройте базу данных

```bash
npm run db:setup
```

Это создаст:
- ✅ Все таблицы (миграции)
- ✅ Админ-пользователя (admin/admin)
- ✅ Категории продуктов

## Шаг 5: Запустите проект

```bash
npm run dev
```

Откройте в браузере:
- **Публичный сайт:** http://localhost:3000
- **Админ-панель:** http://localhost:3000/admin/login
  - Email: `admin`
  - Password: `admin`

## 🎯 Все в одной команде

```bash
npm run setup
```

Эта команда выполнит все шаги автоматически (требует запущенного Docker Desktop).

## 📋 Полезные команды

```bash
# Управление Docker
npm run docker:up      # Запустить PostgreSQL
npm run docker:down     # Остановить PostgreSQL
npm run docker:logs     # Просмотр логов
npm run docker:restart  # Перезапустить

# База данных
npm run db:check       # Проверить DATABASE_URL
npm run db:setup       # Настроить БД
npm run db:migrate     # Выполнить миграции
npm run db:admin       # Создать админ-пользователя

# Разработка
npm run dev            # Запустить dev сервер
npm run build          # Собрать проект
npm run start          # Запустить production сервер
```

## 🐛 Проблемы?

### Docker не запущен
```
Error: permission denied while trying to connect to the Docker daemon socket
```
**Решение:** Запустите Docker Desktop

### Порт 5432 занят
**Решение:** Остановите локальный PostgreSQL:
```bash
brew services stop postgresql@14
```

### DATABASE_URL не найден
**Решение:** Выполните:
```bash
npm run env:update
```

## 📚 Дополнительная документация

- `DOCKER_SETUP.md` - Подробная инструкция по Docker
- `LOCAL_SETUP.md` - Альтернативные способы установки PostgreSQL
- `QUICK_START_LOCAL.md` - Быстрые варианты запуска

## ✅ Готово!

После выполнения всех шагов проект готов к работе!

